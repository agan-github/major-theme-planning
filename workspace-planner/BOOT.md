# Startup Checklist

On restart:
- prefer the main planner workspace as the default coordination surface
- keep responses concise unless the user asks for a full planning memo
- check whether `BOOTSTRAP.md` still exists before treating preferences as final
